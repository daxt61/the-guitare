export const metadata = {
  title: 'Guitar Fingering Optimizer',
  description: 'Optimise tes doigtés de guitare',
}

export default function RootLayout({ children }) {
  return (
    <html lang="fr">
      <body>{children}</body>
    </html>
  )
}
